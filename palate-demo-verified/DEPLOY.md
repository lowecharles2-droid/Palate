# Deploy Palate on Render

Create a **Web Service** with these settings:

- Environment: Node
- Root Directory: `palate-demo-verified` if the folder is inside your repository; otherwise leave it blank
- Build Command: `npm install`
- Start Command: `npm start`

This project uses only built-in Node.js modules and has no external dependencies.
