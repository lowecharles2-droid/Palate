# Deploy to Render

Create a **Web Service** with:

- Environment: Node
- Build Command: `npm install`
- Start Command: `npm start`
- Root Directory: `palate-mvp` if this folder is inside the repository; otherwise leave it blank

No API key or external package is required.
